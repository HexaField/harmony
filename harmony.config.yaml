server:
  host: 0.0.0.0
  port: 3100

storage:
  database: ./harmony.db
  media: ./media
  backup:
    enabled: true
    directory: ./backups
    intervalHours: 168
    maxRetained: 4

voice:
  enabled: true

logging:
  level: info
  format: text
